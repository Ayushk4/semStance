# Remove [?,.!|] while mapping
# Removes DIS_FOXA
# Pad
# Add 1 for CLS and SEP
# Handle apostrophe 's

# Goal:
    # List of node_idx_mapping to tweet["node_idx_map"];
    # Edge_index_list to tweet["edge_idx"]


def 

fo = open("data/normalized.json", "w+")
j = json.load(fo)
fo.close()

